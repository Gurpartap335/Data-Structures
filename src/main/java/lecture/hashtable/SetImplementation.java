package lecture.hashtable;


import java.util.LinkedList;
import java.util.List;

/**
 * HashSet internally creates HashMap.
 * HashSet stores Sets of objects.
 *
 * loadFactor tells when to perform re-hashing. I set it to 75% meaning if we have
 * filled 75% of the container, we need to perform rehash.
 */
public class SetImplementation  {



}
