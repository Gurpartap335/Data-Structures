package lecture.interview;

public class InterviewPractice {

    public static void main(String[] args) {

    }
}


/**
 * Encapsulation
 * You hide (using scope) details about the inner workings of class
 * because the user of the class should not care how it works.
 * Encapsulation isn't necessary for java to "function", but it's a core tenet of
 * well-architected system.
 * How we achieve encapsulation?
 *
 * abstraction
 *
 *
 *
 **/

 /**
  * Access modifiers
 * public accessible from everywhere
 * private accessible within the same class only
 * protected accessible by the classes of the same package and the
 * subclasses residing in any package.
 * package private or default accessible by the classes of the same package
 */
